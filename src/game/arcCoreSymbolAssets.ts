/**
 * 아크코어 상징 정본 — 2001 붉은 렌즈 형상(오리지널 에셋).
 * UI는 `ArcCoreLensIcon`만 사용. 틱·Skia 루프에서 로드 금지.
 */
import type { ImageSourcePropType } from 'react-native';

export const ARC_CORE_LENS_SOURCE: ImageSourcePropType = require('../../assets/images/arcCore/arc_core_lens.png');

export const ARC_CORE_LENS_HEADER_PX = 26;
export const ARC_CORE_LENS_ROSTER_PX = 28;
export const ARC_CORE_LENS_BUBBLE_PX = 16;
