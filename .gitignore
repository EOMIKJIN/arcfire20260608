# --- 필수 제외 항목 (이게 없으면 커서가 멈춥니다) ---
node_modules/
.expo/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- 네이티브 빌드 관련 (npx expo prebuild 시 생성되는 폴더) ---
# 직접 네이티브 코드를 수정하지 않는다면 제외하는 것이 깔끔합니다.
android/
ios/

# --- 환경 변수 및 보안 ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
tools/long-run-monitor/monitor-webhook.local.env

# --- 생성물(은하 그래프 스냅샷; npm run build:world-expansion-graph) ---
tools/world-expansion/master-graph.json

# --- 테스트 및 캐시 ---
coverage/
.eslintcache

# --- OS 및 에디터 관련 ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Expo 관련 추가 패턴 ---
expo-env.d.ts
.expo-shared/
web-build/

# --- 데일리 커밋 로그 (로컬) ---
tools/daily-commit/logs/*.log
tools/kim-team-lead/reports/.kim-claude-auto-review-followup.json

# =============================================================================
# 저장소 위생 (2026-08-09) — 런타임 로그·번들 실험·날짜 아카이브·잔해
# 앱 소스(app/src/tables)·kim-team-lead handoff·reports/latest* 는 추적 유지
# =============================================================================

# 번들/export 실험 산출물
.expo-tmp-bundle-test/
.tmp-bundle-check/
.tmp-expo-export-test/
.tmp/
.runtime-logs/

# 트리/디버그 덤프 · 셸 스텁 잔해
project_structure.txt
/adb
/git
/npx

# 장기 모니터 로그·대시보드 런타임 (PC adb 전용 · 앱 무영향)
tools/long-run-monitor/logs/
tools/long-run-monitor/logs/**
tools/daily-commit/logs/

# 감사·SIM 날짜별 아카이브 (루트 latest.md / learning-state 등은 유지)
tools/**/reports/20*/
tools/**/reports/20*/**

# 프로파일러 타임스탬프 스냅샷 (YYYYMMDD-* · 디스크 보관·git 비추적)
tools/memory-profiler/reports/snapshots/
tools/memory-profiler/reports/snapshots/**

# 휘발성 watch/pid/stderr (balance-ops 등)
tools/**/reports/*.pid
tools/**/reports/watch-*.log
tools/**/reports/audit-last.stdout
tools/**/reports/audit-last.stderr

# 로컬 전용 빈 잔해 폴더 (재생성 방지 힌트)
utils/
tools/story-dialog-audit/
